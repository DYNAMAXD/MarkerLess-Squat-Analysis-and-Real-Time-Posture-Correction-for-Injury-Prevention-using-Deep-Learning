# AlphaPose and MotionBERT are deliberately NOT installed here. Both
# `-e ./AlphaPose` and `-r ./MotionBERT/requirements.txt` fail on HF's
# Gradio-SDK builder: requirements.txt is installed in a layer that runs
# BEFORE your repo's own files are copied in, so any reference to a local
# path in this file will always 404, regardless of what you've pushed.
# app.py installs both, once, at container startup instead — see
# ensure_alphapose_and_motionbert_installed() in app.py.

gradio         # left unpinned — ZeroGPU Spaces auto-inject their own gradio[oauth] pin
pandas>=2.0
opencv-python-headless>=4.8
huggingface_hub>=0.23
ultralytics>=8.3        # YOLOv11 detector — AGPL-3.0, check this fits your project
