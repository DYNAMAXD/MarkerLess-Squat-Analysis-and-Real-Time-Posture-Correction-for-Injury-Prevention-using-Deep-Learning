# ============================================================
# app.py itself
# ============================================================
setuptools<81      # newer setuptools removed pkg_resources; AlphaPose/MotionBERT-era
                    # packages (easydict, halpecocotools, etc.) still import it directly
wheel
gradio>=4.36
numpy
pandas
opencv-python
huggingface_hub
ultralytics

# ============================================================
# AlphaPose — from its own setup.py install_requires + docs/INSTALL.md
# (demo_inference.py needs all of these; natsort is the one you just hit)
# ============================================================
six
terminaltables
scipy
matplotlib
tqdm
tensorboardX
easydict
pyyaml
halpecocotools
munkres
timm==0.1.20
natsort
pycocotools
cython
cython_bbox
ninja
jinja2
typeguard

# ============================================================
# MotionBERT — from its own requirements.txt
# ============================================================
prettytable
chumpy
imageio-ffmpeg
roma
ipdb
smplx[all]        # needed for the mesh step (infer_wild_mesh.py)
pytorch-metric-learning   # only used for one-shot action recognition, harmless to keep

# torch / torchvision: intentionally NOT pinned here — install the build
# that matches this Space's CUDA/driver first (see note below), then this
# file for everything else.